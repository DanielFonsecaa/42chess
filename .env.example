# Example environment file for local builds
# Copy this file to .env in the repo root and replace values with your secrets.
# Do NOT commit your real .env to version control.

# Database connection used by backend at runtime (optional override)
# Example: postgres://user:password@host:5432/dbname
DATABASE_URL=postgresql://postgres:postgres@db:5432/chess42

# JWT secret used by the backend to sign tokens
JWT_TOKEN=replace_this_jwt_secret

# Number of salt rounds for bcrypt (string or integer)
SALT_ROUNDS=12

# Frontend origin allowed by CORS on backend (runtime)
FRONTEND_ORIGIN=http://localhost:8080

# Vite build-time API URL (frontend build arg)
VITE_API_URL=http://localhost:3000

# NOTE: keep your real secrets in a local .env or export them into your shell.